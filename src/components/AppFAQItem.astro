---
interface Props {
  question?: string;
  answer?: string;
}

const { question, answer } = Astro.props;
---

<h2 class="mb-6 text-2xl font-semibold">{question}</h2>
<p class="mb-4">{answer}</p>
<slot />
